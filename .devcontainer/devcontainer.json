{
    "image": "dev-container",
    "runArgs": [
        "-e", "DISPLAY=${localEnv:DISPLAY}",
        "-e", "${DBUS_SESSION_BUS_ADDRESS}",
        "--net=host",
        "--gpus", "all",
        "--privileged"
    ],
    "mounts": [
        "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind",
        "source=/home/miguel/cba_ws/src,target=/root/catkin_ws/src,type=bind"
    ]
}